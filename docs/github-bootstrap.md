# GitHub Bootstrap

Repository:

```text
mhfed/video-workflow-tool
```

## SSH remote

```bash
git@github.com:mhfed/video-workflow-tool.git
```

## Clone

```bash
git clone git@github.com:mhfed/video-workflow-tool.git
cd video-workflow-tool
```

## Upstream whiteboard engine

Keep `geeklee/srt-whiteboard-animation` isolated behind the renderer adapter. For local development, clone it under `vendor/` or add it as a git submodule when implementation begins.

Suggested path:

```text
vendor/srt-whiteboard-animation
```
